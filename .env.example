# Environment Variables Example
# This project uses organization-level secrets for sensitive values
# Only ENVIRONMENT should be set locally

ENVIRONMENT=development
# API_KEY and DATABASE_URL are provided by organization blueprint
# Do not set these values locally - they are injected by Devin Cloud

# SQLAlchemy connection string. Defaults to a local SQLite file when unset.
# DATABASE_URL=sqlite:///./products.db
